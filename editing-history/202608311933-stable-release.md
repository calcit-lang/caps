# Stable 0.1.0 release

- Promoted the cross-project-verified `0.1.0-rc.2` implementation to stable `0.1.0` without functional changes.
- Kept Calcit 0.13.72, `@calcit/procs` 0.13.72, C-safe ABI v1, and `calcit_native_ffi` 0.1.3 as the first supported matrix row.
- Linked the standalone release evidence to the Calcit core cutover tracker #555.
- The stable tag, crates.io artifact, and an isolated installation smoke must be confirmed before removing the duplicate core `caps` binary.
