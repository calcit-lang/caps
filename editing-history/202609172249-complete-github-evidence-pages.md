# Read complete GitHub evidence pages / 完整读取 GitHub 证据页

- Request up to 100 check runs and reviews so reconciliation does not silently use GitHub's shorter default page.
- checks 与 reviews 单次读取上限提高到 100，避免远端对齐静默使用 GitHub 较短的默认分页。
