# Stable release smoke evidence

- Confirmed the `0.1.0` GitHub publish workflow and crates.io index completed successfully.
- Installed `calcit-caps 0.1.0 --locked` into an isolated root and verified `caps --version`.
- In a fresh HOME/module store, Calcium Workflow installed 16 modules and rebuilt `calcit-wss` plus `calcit.std`; tree, status, receipts, Calcit/@calcit-procs 0.13.72 toolchain verification, and Calcit check-only all passed.
- The first run intentionally exposed the isolated HOME's missing Rust toolchain selection; rerunning with explicit `CARGO_HOME`, `RUSTUP_HOME`, and `COREPACK_HOME` verified caps recovery after the interrupted native realization.
