# Prepare 0.1.1 / 准备 0.1.1

- Advanced the standalone caps version to `0.1.1` for the first release carrying the Ubuntu Linux x64 executable and checksum manifest.
- Selected Calcit and `@calcit/procs` `0.14.3` for the corresponding `setup-calcit` integration baseline.
- Kept the `0.1.0` compatibility row as historical release evidence rather than rewriting it.

- 将独立 caps 版本升级到 `0.1.1`，作为首个携带 Ubuntu Linux x64 可执行文件与校验 manifest 的版本。
- 为对应的 `setup-calcit` 集成基线选择 Calcit 与 `@calcit/procs` `0.14.3`。
- 保留 `0.1.0` 兼容矩阵行作为历史发布证据，不改写既有记录。
