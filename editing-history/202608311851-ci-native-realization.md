# Keep native realization in CI mode / CI 模式保留 native realization

- Kept `--ci` scoped to shallow HTTPS Git transport.
- Made normal CI installation build native modules so the project view is runnable.
- Added a regression test that keeps CI transport and native realization independent.
